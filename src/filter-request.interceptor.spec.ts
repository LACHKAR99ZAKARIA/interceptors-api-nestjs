import { FilterRequestInterceptor } from './filter-request.interceptor';

describe('FilterRequestInterceptor', () => {
  it('should be defined', () => {
    expect(new FilterRequestInterceptor()).toBeDefined();
  });
});
