import { EnrichResponseInterceptor } from './enrich-response.interceptor';

describe('EnrichResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new EnrichResponseInterceptor()).toBeDefined();
  });
});
