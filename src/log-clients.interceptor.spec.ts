import { LogClientsInterceptor } from './log-clients.interceptor';

describe('LogClientsInterceptor', () => {
  it('should be defined', () => {
    expect(new LogClientsInterceptor()).toBeDefined();
  });
});
